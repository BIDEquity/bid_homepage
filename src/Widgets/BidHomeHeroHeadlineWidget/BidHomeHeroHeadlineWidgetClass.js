import * as Scrivito from "scrivito";

const BidHomeHeroHeadlineWidget = Scrivito.provideWidgetClass("BidHomeHeroHeadlineWidget", {
  attributes: {
    headline: "string",
  },
  
});

export default BidHomeHeroHeadlineWidget;
