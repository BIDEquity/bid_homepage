import * as Scrivito from "scrivito";

const BidSectionHeadlineWidget = Scrivito.provideWidgetClass("BidSectionHeadlineWidget", {
  attributes: {
    headline: "string",
  },
  
});

export default BidSectionHeadlineWidget;
